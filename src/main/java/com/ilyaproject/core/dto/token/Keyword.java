package com.ilyaproject.core.dto.token;

public enum Keyword {
    SELECT,
    FROM,
    INSERT,
    INTO,
    CREATE,
    TABLE,
    WHERE,
    ORDER,
    BY,
    VALUES,
    AND,
    OR,
    DROP,
    LIMIT
}
